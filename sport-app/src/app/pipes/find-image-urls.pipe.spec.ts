import { FindImageUrlsPipe } from './find-urls.pipe';

describe('FindUrlsPipe', () => {
  it('create an instance', () => {
    const pipe = new FindImageUrlsPipe();
    expect(pipe).toBeTruthy();
  });
});
